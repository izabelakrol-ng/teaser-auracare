{
  "name": "auracare-teaser",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && touch dist/.nojekyll && cd dist && git init -q && git add -A && git commit -qm deploy && git branch -M gh-pages && git push -f \"https://github.com/izabelakrol-ng/teaser-auracare.git\" gh-pages && cd .. && rm -rf dist/.git"
  },
  "dependencies": {
    "@silk/components": "link:/Users/izabela.krol/silk-storybook",
    "@phosphor-icons/react": "^2.1.7",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "tailwindcss": "^4.0.0",
    "tw-animate-css": "^1.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
